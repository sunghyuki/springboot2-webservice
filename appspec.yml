# appspec.yml

version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/springboot2-webservice/
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user